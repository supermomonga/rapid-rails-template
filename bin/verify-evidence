#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../src/rapid_rails_template/evidence"

root = File.expand_path("..", __dir__)
RapidRailsTemplate::Evidence.verify(root: root)
puts "UI evidence is current (#{RapidRailsTemplate::Evidence.fingerprint(root)})"
