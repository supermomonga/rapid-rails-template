| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 候補語 | 初出定義 |
|---|---|---|---|---|---|---|
| 承認済み計画 | ユーザーに認証用識別子を決めさせない | User作成時にサーバーが生成する未使用の20文字小文字hex | 値 | パスワード入力 → User作成 → ID提示 | ユーザーID | ユーザーIDとは、Deviseがpassword認証に使う非公開の`login_id`を指す。 |
| 承認済み計画 | 次回ログインに必要な値を登録直後に保存できるようにする | 登録成功後に認証済みUserの`login_id`を表示する専用画面 | 事象 | User作成 → 自動ログイン → 専用画面へ遷移 | サインアップ完了 | サインアップ完了とは、User作成と自動ログインが成功し、生成されたユーザーIDを本人へ提示する段階を指す。 |
| 承認済み計画 | ログイン中の本人が認証用識別子を再確認できるようにする | アカウント設定内の読み取り専用`login_id`表示 | 手段 | サインアップ完了 → 必要に応じてアカウント設定で再確認 | ユーザーID表示 | ユーザーID表示とは、認証中の本人だけが読み取りとコピーを行える表示を指す。 |
| 承認済み計画 | 生成された認証用識別子を固定する | 永続化済みUserの`login_id`変更を拒否するmodel不変条件 | 状態 | User作成 → 以後の更新で値を維持 | 変更不可 | 変更不可とは、User作成後の`login_id`変更をmodel validationで拒否する状態を指す。 |
