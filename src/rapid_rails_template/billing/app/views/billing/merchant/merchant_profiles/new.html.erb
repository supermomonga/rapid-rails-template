<% content_for :page_title, t("billing.ui.merchant_registration") %>
<div class="card card-border bg-base-100"><div class="card-body p-3">
  <h2 class="card-title"><%= billing_label("start_selling", "building-storefront") %></h2>
  <p><%= t("billing.ui.registration_help") %></p>
  <%= render "form" %>
</div></div>
